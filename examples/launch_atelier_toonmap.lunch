#!/usr/bin/env lunch
"""
Lunch script for Toonloop + MapMap
Atelier ToonMap de Perte de Signal
"""
add_command(os.path.expanduser("~/src/toonmap/scripts/toonmap"))
add_command(os.path.expanduser("~/src/toonmap/scripts/toonloop-wizard"))
add_command("mapmap -f ~/src/toonmap/examples/atelier-toonmap.mmp")
# add_command("v4l2ucp %s" % (V4L2_DEVICE))

